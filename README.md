只关注src目录下的components里的controller.jsx文件就行
**父组件使用props传入src、alt和style**
father:
import Controller from '*******controller'

function Father () {
  return (<Controller src="https*********" alt="*********" style="{width: 100px}"/>)
}
